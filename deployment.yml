apiVersion: apps/v1
kind: Deployment
metadata:
  name: Lab4-JM-SYSADM-kuber-project
  labels:
    app: kuber-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kuber-project
  template:
    metadata:
      labels: 
        app: kuber-project
    
    spec:
      containers:
      - name: result-app
        image: REPOSITORY_TAG
        ports:
        - containerPort: 3000
